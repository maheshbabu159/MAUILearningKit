<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MAUILearningKit.ViewModels"
             x:Class="MAUILearningKit.Views.OrdersPage"
             x:DataType="viewmodels:OrdersViewModel">
    <VerticalStackLayout>
        <Button Text="Load Orders" Command="{Binding LoadOrdersCommand}" />
        <CollectionView ItemsSource="{Binding Orders}" SelectionMode="Single" SelectedItem="{Binding SelectedCustomer}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame  BorderColor="DarkGray"
                            CornerRadius="5"
                            Margin="20"
                            HeightRequest="360"
                            HorizontalOptions="Center"
                            VerticalOptions="Center">
                        <Label Text="{Binding ShipName}" />
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>